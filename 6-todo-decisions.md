# TODO / 아직 결정하지 않은 사항

이 문서는 현재 프로젝트에서
아직 확정하지 않은 사항을 기록한다.

Claude Code는 이 문서에 기록된 사항을
사용자의 승인 없이 임의로 확정하지 않는다.


## 1. 강도 조정폭

"가볍게 / 권장 / 강하게"를 선택했을 때
실제 페이스, 반복수, 회복시간 등을
얼마나 조정할지는 아직 결정하지 않았다.

연구 검증 없이 임의의 % 또는 초를 확정하지 않는다.


## 2. 실패 후 조정 기준

몇 회 실패하면 훈련을 변경할지는 아직 결정하지 않았다.

현재 원칙:

한 번 실패했다고 즉시 크게 낮추지 않는다.

반복되는 실패 패턴을 확인한 뒤
처방 변경을 고려한다.

정확한 횟수는 추후 연구 및 자체 데이터로 검증한다.


## 3. 성공 후 상향 기준

한 번 성공했다고
다음 훈련 강도를 크게 높이지 않는다.

RPE, 후반 페이스 저하, 회복시간,
심박수 등을 함께 고려한다.

정확한 상향폭은 아직 결정하지 않았다.


## 4. 대체훈련 개수

현재 동일 목적의 대체훈련을
최대 2개 정도 제공하는 방향을 고려한다.

그러나 이것이 과학적으로 최적이라는 의미는 아니다.

실제 사용성을 확인하면서 조정한다.


## 5. 장기 계획 기간

개념적 단계:

기초
→ 향상
→ 2km 특이적
→ 시험 대비
→ 테이퍼

각 단계의 정확한 주차는 아직 결정하지 않았다.


## 6. 주간 훈련 빈도

사용자가 일주일에 몇 번 훈련하는 것이
최적인지는 개인별로 달라질 수 있다.

현재 사용자가 입력한 가능한 훈련 횟수를
계획 생성의 입력값으로 사용하는 방향을 고려한다.

정확한 훈련 빈도 알고리즘은 추후 결정한다.


## 7. 컨디션 입력

현재는 세부적인:

- 수면
- 스트레스
- 피로
- 환경
- 심리 상태

등을 각각 입력하게 하지 않는다.

향후 "컨디션" 하나로 묶어 제공하는 방향을 우선 검토한다.


## 8. 심박수 활용

평균 심박수와 최대 심박수를 기록할 수 있도록 한다.

그러나 심박수 하나만으로
훈련 강도를 자동 변경하지 않는다.

향후 안정시/야간 HR과 운동 중 HR 등을
구분할 필요가 있는지 연구한다.


## 9. 고급 신체 데이터

향후 다음 데이터를 추가할 수 있다.

- 신장
- 체중
- 체지방률
- 케이던스
- 훈련 경력

그러나 데이터를 추가한다고
개인화 정확도가 자동으로 올라간다고 가정하지 않는다.

실제로 AI의 판단을 개선하는지 확인한 뒤 추가한다.


## 10. 부상 데이터

장기적인 통증 및 부상 패턴을
향후 코칭에 활용할 수 있다.

단순히 "통증 있음/없음"만으로
훈련 처방을 자동 결정하는 것은 아직 확정하지 않았다.

안전과 관련된 레드 플래그 기준도
추후 별도 설계한다.


## 11. 시험일

시험일을 입력하고 D-day를 계산하는 기능을
최종 시스템에 포함한다.

현재 실제 시험일이 확정되지 않은 경우
사용자가 직접 입력할 수 있도록 한다.


## 12. 실제 AI API

현재 JavaScript 추천 로직은 프로토타입이다.

향후 실제 AI API를 연결할 예정이다.

어떤 AI 모델을 사용할지는 아직 결정하지 않았다.

API 연결 전까지는
데이터 구조와 코칭 규칙을 먼저 정교화한다.


## 13. AI의 웹 검색 / 연구 검색

최종적으로 AI가 최신 연구와 논문,
실제 훈련 방법 등을 검색하여
코칭 판단에 활용하는 구조를 고려한다.

현재는 실제 API 연결 전이므로
검색 기능을 코드에 억지로 구현하지 않는다.


## 14. 코칭 알고리즘

현재 알고리즘은 최종 버전이 아니다.

특히 다음 부분은 향후 연구와 실제 사용자 데이터를 통해
검증해야 한다.

- 현재 능력 산정
- 목표와의 격차 산정
- 훈련 목적 선택
- 훈련 후보 선택
- 강도 선택
- 성공/실패 판단
- 반복 실패 처리
- 성공 후 상향
- 장기 계획
- 시험 전 테이퍼


## 15. 자체 데이터 검증

최종적으로는 실제 사용자들의 훈련 데이터를 축적하여

추천
→ 수행
→ 결과
→ 다음 추천
→ 실제 기록 변화

의 관계를 분석해야 한다.

이를 통해 연구에서 직접 검증되지 않은
세부적인 코칭 파라미터를 자체적으로 검증할 수 있다.


# 변경 규칙

새로운 결정이 내려지면
해당 결정은 이 파일에서 제거하고
관련 공식 문서에 반영한다.

예:

코칭 규칙 변경
→ COACH_RULES.md

연구 근거 변경
→ RESEARCH_STATUS.md

데이터 구조 변경
→ DATA_MODEL.md

UI 원칙 변경
→ UI_UX_RULES.md

프로젝트 방향 변경
→ PROJECT_CONTEXT.md


# 중요

이 파일에 남아 있는 내용은
아직 확정되지 않은 것이다.

Claude Code는 사용자의 명시적인 승인 없이
TODO에 있는 내용을 코드에 확정적으로 구현하지 않는다.