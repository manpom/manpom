# TODO / 아직 결정하지 않은 사항

이 문서는 현재 프로젝트에서
아직 확정하지 않은 사항을 기록한다.

Claude Code는 이 문서에 기록된 사항을
사용자의 승인 없이 임의로 확정하지 않는다.


## 1. 강도 조정폭

"가볍게 / 권장 / 강하게"를 선택했을 때
실제 페이스, 반복수, 회복시간 등을
얼마나 조정할지는 아직 결정하지 않았다.

연구 검증 없이 임의의 % 또는 초를 확정하지 않는다.


## 2. 실패 후 조정 기준

몇 회 실패하면 훈련을 변경할지는 아직 과학적으로 확정하지 않았다.

**2026-08 갱신**: 코드에는 "연속 실패 2회"를 반복 실패 판정의
1차 휴리스틱으로 구현했다(`2-coach-rules.md` §24.3). 이 "2"는
자체 데이터로 검증된 값이 아니라 자리를 채우기 위한 운영값이므로
계속 재검증 대상이다. 사용자와 함께 실제 데이터를 보며 조정한다.

**2026-08 2차 갱신**: 실사용 테스트에서 "실패한 세션보다 많은 세트를
다음에 처방"하는 논리 오류가 발견되어, 세트 상한을 실제로 완료를
증명한 세트 수(`lastSuccessSet`)로 고정했다(`2-coach-rules.md` §25).
이건 "몇 회 실패해야 조정하는가"와는 다른 축의 문제(로직 버그)였고,
"몇 회" 자체는 여전히 미확정이다.

현재 원칙(유지):

한 번 실패했다고 즉시 크게 낮추지 않는다.

반복되는 실패 패턴을 확인한 뒤
처방 변경을 고려한다.

실패한 세션보다 많은 세트를 다음 목표로 주지 않는다.


## 3. 성공 후 상향 기준

한 번 성공했다고
다음 훈련 강도를 크게 높이지 않는다.

RPE, 후반 페이스 저하, 회복시간,
심박수 등을 함께 고려한다.

**2026-08 갱신**: 코드에는 "RPE 9 이상이면 안정적 성공이어도 상향
보류"를 1차 휴리스틱으로 구현했다(`2-coach-rules.md` §24.3). "9"는
1~10 RPE 척도의 통상적 해석을 참고한 값일 뿐 연구로 검증된 임계값이
아니다.

**2026-08 2차 갱신**: RPE 6과 RPE 8이 동일하게 처리되는 문제가
실사용 테스트(Test D)에서 발견되어, RPE를 여유(≤6)/중간(7~8)/높음(≥9)
3구간으로 세분화했다(`2-coach-rules.md` §26). 여유 구간만 기존 발전
로직(세트+1 등)을 적용하고, 중간·높음은 둘 다 보류한다. 경계값(6, 9)과
"중간=보류"라는 처리 방식 모두 연구로 검증된 것이 아니라 문제 해소를
위한 1차 휴리스틱이다. 정확한 상향폭(세트 증가량, 페이스 단축폭)은
여전히 결정하지 않았다.


## 4. 대체훈련 개수

현재 동일 목적의 대체훈련을
최대 2개 정도 제공하는 방향을 고려한다.

그러나 이것이 과학적으로 최적이라는 의미는 아니다.

실제 사용성을 확인하면서 조정한다.


## 5. 장기 계획 기간

개념적 단계:

기초
→ 향상
→ 2km 특이적
→ 시험 대비
→ 테이퍼

각 단계의 정확한 주차는 아직 결정하지 않았다.


## 6. 주간 훈련 빈도

사용자가 일주일에 몇 번 훈련하는 것이
최적인지는 개인별로 달라질 수 있다.

**2026-08 4차 갱신**: "주당 러닝 가능 횟수" 사용자 입력 UI는 삭제했다
(5-ui-ux-rules.md §15). 대신 코드 상수 `DEFAULT_WEEKLY_FREQ=3`(주 3회)을
대상군 특성을 참고한 초기 운영 가정으로 고정했다(2-coach-rules.md §31).
이 3이라는 숫자와 "주 3회면 목표 기록이 나온다"는 식의 보장은 여전히
검증된 것이 아니다.

시험일까지 남은 기간과 실제 수행 반응(성공/실패 패턴, RPE 등)에 따라
빈도를 조정하는 개인화 알고리즘은 아직 만들지 않았다 — 정확한 알고리즘은
추후 결정한다.


## 7. 컨디션 입력

현재는 세부적인:

- 수면
- 스트레스
- 피로
- 환경
- 심리 상태

등을 각각 입력하게 하지 않는다.

향후 "컨디션" 하나로 묶어 제공하는 방향을 우선 검토한다.


## 8. 심박수 활용

평균 심박수와 최대 심박수를 기록할 수 있도록 한다.

그러나 심박수 하나만으로
훈련 강도를 자동 변경하지 않는다.

향후 안정시/야간 HR과 운동 중 HR 등을
구분할 필요가 있는지 연구한다.


## 9. 고급 신체 데이터

향후 다음 데이터를 추가할 수 있다.

- 신장
- 체중
- 체지방률
- 케이던스
- 훈련 경력

그러나 데이터를 추가한다고
개인화 정확도가 자동으로 올라간다고 가정하지 않는다.

실제로 AI의 판단을 개선하는지 확인한 뒤 추가한다.


## 10. 부상 데이터

장기적인 통증 및 부상 패턴을
향후 코칭에 활용할 수 있다.

단순히 "통증 있음/없음"만으로
훈련 처방을 자동 결정하는 것은 아직 확정하지 않았다.

안전과 관련된 레드 플래그 기준도
추후 별도 설계한다.


## 11. 시험일

시험일을 입력하고 D-day를 계산하는 기능을
최종 시스템에 포함한다.

현재 실제 시험일이 확정되지 않은 경우
사용자가 직접 입력할 수 있도록 한다.


## 12. 실제 AI API

현재 JavaScript 추천 로직은 프로토타입이다.

향후 실제 AI API를 연결할 예정이다.

어떤 AI 모델을 사용할지는 아직 결정하지 않았다.

API 연결 전까지는
데이터 구조와 코칭 규칙을 먼저 정교화한다.


## 13. AI의 웹 검색 / 연구 검색

최종적으로 AI가 최신 연구와 논문,
실제 훈련 방법 등을 검색하여
코칭 판단에 활용하는 구조를 고려한다.

현재는 실제 API 연결 전이므로
검색 기능을 코드에 억지로 구현하지 않는다.


## 14. 코칭 알고리즘

현재 알고리즘은 최종 버전이 아니다.

특히 다음 부분은 향후 연구와 실제 사용자 데이터를 통해
검증해야 한다.

- 현재 능력 산정
- 목표와의 격차 산정
- 훈련 목적 선택
- 훈련 후보 선택
- 강도 선택
- 성공/실패 판단
- 반복 실패 처리
- 성공 후 상향
- 장기 계획
- 시험 전 테이퍼


## 15. 자체 데이터 검증

최종적으로는 실제 사용자들의 훈련 데이터를 축적하여

추천
→ 수행
→ 결과
→ 다음 추천
→ 실제 기록 변화

의 관계를 분석해야 한다.

이를 통해 연구에서 직접 검증되지 않은
세부적인 코칭 파라미터를 자체적으로 검증할 수 있다.


## 16. 초기 세트수의 볼륨 참고 비율 (2026-08 추가)

훈련 이력이 없을 때 초기 세트수를 "TT 거리 대비 볼륨 비율"로 계산하도록
1차 구현했다(`INITIAL_VOLUME_RATIO_TO_TT = 1.0`).

이 배율이 1.0이 적절한지, 목적(고강도/중강도)별로 달라야 하는지는
아직 결정하지 않았다. 사용자 실사용 데이터가 쌓이면 재검토한다.


## 17. 종목 확장 시점

현재는 2km 전용 앱으로 정리했다(다른 5종목 화면·데이터 제거).

`1-project-context.md`의 "6개 종목 최종 관리" 방향 자체는 유지하되,
언제·어떤 순서로 나머지 종목을 다시 추가할지는 아직 결정하지 않았다.


## 18. 인터벌 페이스 공식의 weight 값

`estimateInitialPaceForUnit`의 `weight=0.8`은 목표 격차를 얼마나
반영할지 정하는 값이다. 절대 상한(clamp)을 없애고 이 값 하나로 격차
반영 정도를 조절하는 구조로 바꿨지만, 0.8이 적절한지는 검증하지
않았다. 사용자 데이터가 쌓이면 재검토한다.


## 19. 인터벌 4축·지속주 2축 로테이션의 순서와 조정폭

`2-coach-rules.md` §28.2(인터벌 4축: 페이스→밀도→볼륨→구조)와 §29
(지속주 2축: 지속시간/페이스)의 로테이션 순서, 그리고 각 축의 조정폭
(페이스 1초, 회복 5초, 세트 1개, 지속시간 2분 등)은 전부 1차
휴리스틱이다. "이 순서가 최적"이라는 근거는 없다 — 사용자 실사용
데이터로 검증해야 할 대상이다. `THRESHOLD_DURATION_MIN_RANGE`(10~20분),
`CRUISE_SEGMENT_MIN_RANGE`(5~8분), `CRUISE_REST_TEXT`(75초)도 동일하게
1차 운영값이다.


## 20. 역치 페이스 배율 (2026-08 4차 추가)

지속주·크루즈 인터벌의 초기 페이스를 "2km TT 페이스 × 1.15"로 계산하도록
바꿨다(`THRESHOLD_PACE_RATIO_TO_TT`, 2-coach-rules.md §30). 이 1.15는
2000m≈105%MAS(직접 근거)와 Daniels T-pace≈88~92%vVO2max(인접 근거,
5K~마라톤 캘리브레이션의 외삽)를 결합해 계산한 값(약 14~19% 범위의
하단)이며, 정밀하게 검증된 배율은 아니다.

사용자 실사용 데이터가 쌓이면(지속주 이력 기반 조정이 누적되면) 이
초기값 대신 실제 수행 데이터를 우선하게 되지만, "1.15가 이 대상군에게
적절한 시작점인가" 자체는 아직 검증되지 않았다 — 사용자와 함께 실제
지속주 수행 결과(성공/실패, 체감 난이도)를 보며 재검토한다.


## 21. 현장 Benchmark 기반 Sanity Check 도입 여부 (2026-08 5차 추가)

`3-research-status.md` §18.7·`2-coach-rules.md` §35.2에서 "현장
Benchmark와 알고리즘 결과를 비교해서 지나치게 동떨어진 처방을 감지하는
장치(sanity check)"의 필요성을 논의했지만, **아직 코드에 구현하지
않았다.** 다음이 전부 미정이다.

- 자동 감지를 코드에 넣을지, 아니면 이 문서처럼 사람이 주기적으로
  비교하는 수준으로 둘지
- 넣는다면 "지나치게 낮음/높음"을 가르는 구체적 임계값(예: Benchmark
  범위에서 몇 % 벗어나면 경고할지)
- 경고가 떴을 때 사용자에게 어떻게 보여줄지(처방을 막을지, 참고 문구만
  붙일지)

사용자 승인 없이 임의로 구현하지 않는다.


## 22. 현재 알고리즘 초기값과 현장 Benchmark의 격차 (2026-08 5차 추가)

`3-research-status.md` §19에서 비교한 결과, 코드 수정 없이 다음
항목들을 "검토 대상"으로만 남긴다.

- **400m 인터벌 회복시간**: 코드 상수 `restText:'2분'`(120초)이 현장
  Benchmark C(60~80초)보다 뚜렷하게 길다. 이 값은 애초에 연구·Benchmark
  어느 쪽에서도 나온 값이 아니라 코드 작성 시점의 임의값이었다
  (3-research-status.md §9 "근거가 부족한 숫자"에 있던 항목) — 이번
  비교로 구체적인 격차가 드러났다. 60~80초로 낮출지, 다른 값으로 할지는
  사용자 승인 후 결정한다.
- **400m 인터벌 초기 세트수(5)**: Benchmark 하단(5)과는 일치하지만
  상단(7~8)과는 거리가 있다. "보수적으로 시작"이라는 기존 설계 철학
  (2-coach-rules.md §9·§24.1)과 "이 대상군은 더 높은 데서 시작해도
  되는 경우가 많다"는 Benchmark 신호가 상충한다 — 초기값을 올릴지,
  대신 진행(progression) 속도를 빠르게 할지는 아직 결정하지 않았다.
- **1200m 반복 훈련**: 현재 앱에는 1200m interval variant 자체가 없다
  (SESSION_VARIANTS.interval에는 400m·800m만 있음). Benchmark A·B는
  1200m 데이터를 포함하므로, 1200m variant를 새로 추가할지는 향후
  검토 대상이다 — 지금은 추가하지 않는다.
- **800m 회복시간·세트수**: 비교할 Benchmark 자체가 없어 "맞다/틀리다"를
  판단할 근거가 없다(2026-08 7차 갱신: 이전에 "Benchmark D 2:40×3세트"와
  비교했던 것은 원자료 오독이었다 — 3-research-status.md §18.5 정정
  참고). 데이터가 더 쌓이기 전까지는 현행 유지.
- **지속주 페이스**: (2026-08 6차 갱신) Benchmark E(T1·T2)가 생겼지만
  세션 길이가 달라(10~20분 vs 45~50분) 직접 비교할 수 없다 — 자세한
  내용은 §23 참고.

이 항목들은 전부 **문서화만 된 상태**이며, 사용자 승인 없이 코드에
반영하지 않는다.


## 23. 지속주 페이스 산출식의 지속시간 전제 불일치 (2026-08 6차 추가)

`3-research-status.md` §20에서 확인한 내용을 TODO로 옮긴다.

`THRESHOLD_PACE_RATIO_TO_TT=1.15`의 근거였던 Daniels T-pace는 "약
50~60분 유지 가능한 페이스"로 정의되는데, 코드는 이 값을 **10~20분**
지속주 세션(Trahere 자료 기반, 2-coach-rules.md §29)에 그대로 쓰고
있다 — 페이스 산출식이 전제하는 지속시간과 실제 세션 길이가 서로
다르다는 잠재적 불일치가 이번 Benchmark E 비교 과정에서 드러났다.

미정 사항:

- 10~20분용 페이스와 45~60분용 페이스를 코드에서 분리된 변수로
  관리할지
- 지속시간-페이스 관계(Critical Speed/D′ 등)를 이 대상군 데이터로
  캘리브레이션해서 세션 길이별 페이스를 계산할지, 아니면 지금처럼
  하나의 값으로 단순화해서 쓸지
- Benchmark T1(45분·270초/km)을 "더 긴 세션용 참고 페이스"로 코드에
  별도로 반영할지

**지금은 판단할 데이터가 부족하다.** 5km/10km 기록 + 체중 + 실제
지속주 수행 데이터가 여러 명·여러 시점에서 쌓이기 전까지는 코드를
수정하지 않는다(4-data-model.md §13 참고).


## 24. 체중과 지속주 수행능력의 관계 — 가설 상태 유지 (2026-08 6차 추가)

Benchmark T1(74kg·4:30/km, 확정)과 T2(84kg·약 5:00/km, 추정) 두 지점을
근거로 "체중이 지속주 수행능력에 영향을 줄 수 있다"는 가설을
`3-research-status.md` §18.8에 기록했다.

**절대 하지 않을 것**: "체중 1kg당 몇 초/km" 같은 보정계수를 만드는
것. 이유:

- 데이터가 T1·T2 두 지점뿐이다.
- 두 지점 사이에는 체중 변화 외에도 시간 경과에 따른 훈련량·컨디션
  변화가 함께 끼어 있어, 체중의 순수한 효과만 분리할 수 없다.
- T2는 애초에 추정치이지 실측값이 아니다.

앞으로 여러 사람·여러 시점의 (2km/5km/10km 기록 + 체중 + 실제 지속주
수행) 데이터가 쌓이면 이 가설을 실제로 검증할 수 있다
(4-data-model.md §13-2). 그 전까지는 가설 상태로만 유지하고, 코드에
체중 기반 보정 로직을 추가하지 않는다.


# 변경 규칙

새로운 결정이 내려지면
해당 결정은 이 파일에서 제거하고
관련 공식 문서에 반영한다.

예:

코칭 규칙 변경
→ COACH_RULES.md

연구 근거 변경
→ RESEARCH_STATUS.md

데이터 구조 변경
→ DATA_MODEL.md

UI 원칙 변경
→ UI_UX_RULES.md

프로젝트 방향 변경
→ PROJECT_CONTEXT.md


# 중요

이 파일에 남아 있는 내용은
아직 확정되지 않은 것이다.

Claude Code는 사용자의 명시적인 승인 없이
TODO에 있는 내용을 코드에 확정적으로 구현하지 않는다.